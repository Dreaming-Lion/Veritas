# =========================
# Veritas monorepo .gitignore
# (frontend: Vite+React+TS+Tailwind, backend: Spring Boot+Gradle, ai: FastAPI)
# =========================

# -------- Logs (all)
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
*.pid
*.seed
*.pid.lock

# -------- OS junk
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
Icon?
*.sw?
*~
# mac tarball noise
._*

# -------- IDE/editor
.vscode/*
!.vscode/extensions.json
.idea
*.iml
.project
.classpath
.settings/

# -------- Root/env
.env
.env.*
!.env.example
!.env.*.example

# =========================
# Frontend (Vite + React + TS + Tailwind)
# =========================

# dependencies
frontend/node_modules
node_modules

# build output
dist
dist-ssr
*.local

# Vite/TS caches
.vite
vite.config.ts.timestamp-*
tsconfig.tsbuildinfo
*.tsbuildinfo
.eslintcache

# coverage / test output
coverage
coverage-*
.nyc_output
jest-html-reporters-*

# PostCSS/Tailwind (v4 uses @tailwindcss/postcss; no extra cache files by default)

# =========================
# Backend (Spring Boot + Gradle)
# =========================

# Gradle
.gradle/
**/.gradle/
build/
**/build/
out/
**/out/

# Spring Boot native/build tooling
spring.log
hs_err_pid*.log
replay_pid*.log

# Keep Gradle wrapper in repo (do NOT ignore)
# (Just in case: ensure these are NOT ignored)
!gradlew
!gradlew.bat
!gradle/wrapper/gradle-wrapper.jar
!gradle/wrapper/gradle-wrapper.properties

# Maven (if any module uses it)
target/
.mvn/timing.properties
# Keep maven wrapper
!mvnw
!mvnw.cmd
!.mvn/wrapper/maven-wrapper.jar
!.mvn/wrapper/maven-wrapper.properties

# IntelliJ annotation processing
.backend/**/generated/
**/generated/

# =========================
# AI (Python FastAPI)
# =========================

# Python bytecode / cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Python envs
.venv
venv
env
ENV
venv.bak/
env.bak/
.python-version

# Python packaging / build
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
.cache
htmlcov/

# Jupyter
.ipynb_checkpoints/

# =========================
# Docker / Compose
# =========================
docker-compose.override.yml
docker-compose.*.local.yml
docker-data/
tmp/
*.tmp

# Named volumes are managed by Docker; if you also keep local data dirs, ignore them here:
db-data/
frontend-node-modules/
backend-gradle-cache/
ai-model-cache/

# =========================
# Misc
# =========================
# lockfiles SHOULD be committed (npm/yarn/pnpm), so do not ignore:
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# compressed archives
*.zip
*.tar
*.tgz
*.gz
